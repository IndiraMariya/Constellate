{
  "name": "constellate-tauri",
  "version": "1.0.0",
  "private": true,
  "description": "Constellate — a calm task app where tasks are stars, as a native macOS app via Tauri.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.0"
  }
}
